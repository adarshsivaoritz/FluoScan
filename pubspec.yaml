name: fluoscan
publish_to: "none"
description: Camera-only Flutter Web reader for fluorescent Code 128 barcode photographs displayed on a screen.
version: 0.4.0+4

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
